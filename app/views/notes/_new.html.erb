<%= form_for Note.new, :remote => true do |f| %>
  <div class="field">
    <%= f.text_area(:contents, :rows => 2, :style => "width: 390px; padding: 10px;") %>
  </div>

  <div class="actions">
    <%= f.submit "Add note", :class => "btn"%>
  </div>
<% end %>